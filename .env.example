# MoonGate MCP Server - Environment Variables
# Copy this file to .env and fill in your values

# Manual JWT token for testing (get from wallet.moongate.one Local Storage)
MOONGATE_TOKEN=your-jwt-token-here

# Enable debug logging (must be exactly "true" for debug logs)
# Note: When using MCP Inspector (pnpm run dev), logs appear in the Inspector web UI's Notifications pane, not the terminal
MOONGATE_MCP_DEBUG=true

# OAuth callback port (default: 8787)
# MOONGATE_CALLBACK_PORT=8787

# API base URL (default: https://wallet.moongate.one)
# MOONGATE_API_URL=https://wallet.moongate.one
